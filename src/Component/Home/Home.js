import React from 'react';
import BookCard from '../BookCard/BookCard';
import Header from '../Header/Header';

const Home = () => {

    return (
        <div>
            <Header></Header>
            <BookCard></BookCard>
        </div>
    );
};

export default Home;